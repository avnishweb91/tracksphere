import { Box, Card, CardContent, Chip, Grid, LinearProgress, Stack, Typography } from '@mui/material'
import { CheckCircleOutline, LocalShipping, Route as RouteIcon } from '@mui/icons-material'
import AppShell from '../components/AppShell'
import LocationMap from '../components/LocationMap'
import { useLiveLocations } from '../hooks/useLiveLocations'
export default function EmployeeDashboardPage() { const { locations, connected } = useLiveLocations(); const uniqueVehicles = new Set(locations.map(item => item.vehicleId)).size; return <AppShell><Box sx={{ mb: 4 }}><Stack direction={{ xs: 'column', sm: 'row' }} justifyContent="space-between" spacing={2}><Box><Typography variant="h4">Fleet, at a glance.</Typography><Typography color="text.secondary">A real-time view of movement across your active operation.</Typography></Box><Chip icon={<CheckCircleOutline/>} label={connected ? 'Live telemetry connected' : 'Reconnecting telemetry'} color={connected ? 'success' : 'warning'} variant="outlined" sx={{ alignSelf: { sm: 'center' } }}/></Stack></Box><Grid container spacing={2.5} sx={{ mb: 3 }}><Grid size={{ xs: 12, sm: 6, lg: 3 }}><Card><CardContent><LocalShipping color="primary"/><Typography variant="h4" sx={{ mt: 1 }}>{uniqueVehicles}</Typography><Typography color="text.secondary" variant="body2">Vehicles in live feed</Typography></CardContent></Card></Grid><Grid size={{ xs: 12, sm: 6, lg: 3 }}><Card><CardContent><RouteIcon color="secondary"/><Typography variant="h4" sx={{ mt: 1 }}>98.6%</Typography><Typography color="text.secondary" variant="body2">Route compliance today</Typography></CardContent></Card></Grid><Grid size={{ xs: 12, sm: 6, lg: 3 }}><Card><CardContent><CheckCircleOutline color="success"/><Typography variant="h4" sx={{ mt: 1 }}>0</Typography><Typography color="text.secondary" variant="body2">Critical alerts open</Typography></CardContent></Card></Grid><Grid size={{ xs: 12, sm: 6, lg: 3 }}><Card><CardContent><Typography variant="overline" color="text.secondary">Network health</Typography><Typography variant="h5">Excellent</Typography><LinearProgress variant="determinate" value={96} color="success" sx={{ mt: 1.5, height: 8, borderRadius: 4 }}/></CardContent></Card></Grid></Grid><Grid container spacing={3}><Grid size={{ xs: 12, lg: 8 }}><Card variant="outlined"><CardContent><Stack direction="row" justifyContent="space-between" alignItems="center" sx={{ mb: 1 }}><Typography variant="h6">Live vehicle map</Typography><Typography variant="caption" color="text.secondary">Updates automatically</Typography></Stack><LocationMap locations={locations}/></CardContent></Card></Grid><Grid size={{ xs: 12, lg: 4 }}><Card variant="outlined" sx={{ height: '100%' }}><CardContent><Typography variant="h6" gutterBottom>Latest movement</Typography><Stack spacing={2}>{locations.slice(0, 5).map(location => <Box key={location.id} sx={{ p: 1.25, bgcolor: 'grey.50', borderRadius: 2 }}><Stack direction="row" justifyContent="space-between"><Typography fontWeight={750}>{location.vehicleId}</Typography><Chip size="small" label="Moving" color="success" variant="outlined"/></Stack><Typography variant="body2" color="text.secondary">{location.latitude.toFixed(5)}, {location.longitude.toFixed(5)}</Typography><Typography variant="caption" color="text.secondary">Updated {new Date(location.recordedAt).toLocaleString()}</Typography></Box>)}{!locations.length && <Typography color="text.secondary">No vehicle locations have been received yet.</Typography>}</Stack></CardContent></Card></Grid></Grid></AppShell> }
