# TrackSphere Frontend

React 19 + Vite dashboard with Material UI and JWT-based authentication.

```bash
cp .env.example .env
npm install
npm run dev
```

The default API target is `http://localhost:8080/api/v1`.
